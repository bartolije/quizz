# Forcer l'installation des devDependencies même quand NODE_ENV=production
# (cas des builds Vercel / Railway). tsc, vite, tsx sont des devDependencies
# nécessaires au build du monorepo.
include=dev
