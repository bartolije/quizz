# Audit robustesse — juillet 2026 (préparation soirée)

Audit complet (architecture + réseau + charge + UX + sécurité) réalisé le 01-02/07/2026
en vue d'une soirée d'entreprise à fort enjeu (~20-80 joueurs, wifi d'entreprise + 4G).
Constats vérifiés ligne par ligne dans le code. Ce fichier sert de **checklist de
correction** : cocher au fur et à mesure des commits.

## Verdict global

Architecture saine, code soigné. La reconnexion *participant* est bien conçue
(token localStorage, `session_restored` complet, timer ancré horloge client).
Les points faibles réels : le ré-attachement **host/TV**, le scénario **restart
serveur**, et l'absence d'**accusé de réception** des réponses.
Charge : un process Node tient 80 joueurs × 20 questions avec ≥ 10× de marge.

## P0 — correctifs avant la soirée

- [x] **Infra de tests** (préalable) : vitest + extraction `socket-handlers.ts` +
  30 tests (scoring, store, intégration Socket.io réelle). `npm test`.
- [x] **P0-1 CRITIQUE — host/TV morts après une micro-coupure.**
  `useHostSession.ts` : `socket.once('connect', join)` → le `host_join` n'est ré-émis
  qu'à la première connexion ; or `disconnect.ts` retire le socket de `hostSocketIds`
  et des rooms. Boutons morts + TV figée, sans erreur. **Fixé** : `socket.on('connect', join)`
  + bandeau `ConnectionBanner` (control + TV) + tests `host-reattach.test.ts`.
- [x] **P0-2 CRITIQUE — téléphones gelés après un restart serveur.**
  `ParticipantApp.tsx` : le listener `INVALID_TOKEN` n'est actif que quand `myId` est
  null. En pleine partie, l'erreur n'est écoutée par personne → UI figée à jamais.
  **Fixé** : `onSessionLost` dans le store (reset + `fatalNotice`), écoute permanente
  dans `useParticipantEvents`, message explicatif sur /join. Bonus : leak des
  listeners `once` de JoinPage corrigé (détachés à chaque réponse/timeout/unmount).
- [x] **P0-3 HAUTE — « Réponse envoyée ✓ » sans ack.**
  `QuestionPage.tsx` : emit optimiste ; une réponse émise pendant/juste avant une
  coupure est perdue en silence (le buffer part avant le rejoin → `socket.id` inconnu).
  **Fixé** : ack Socket.io (`SubmitAnswerAck`) + `submitAnswerReliably` (5 essais,
  timeout 2 s, pause si rejoin en cours) + états visuels envoi/envoyée/trop tard/
  échec+réessayer. Bonus : `questionIndex` dans le payload — une réponse retardée
  rejouée par le buffer ne peut plus être comptée pour la question SUIVANTE.
- [x] **P0-4 HAUTE — resync incomplet hors question ouverte.**
  `quiz-store.ts onSessionRestored` : reconnexion pendant révélation/classement →
  vue « question » périmée figée ; si dernière question → podium jamais affiché.
  **Fixé** : `session_restored` enrichi (`scores` + `lastResult` individuel,
  mémorisé dans `session.lastQuestionResults`), bascule de vue correcte
  (question périmée → révélation ; ended → podium ; blip → vue conservée).
  Bonus TV : phase `interlude` (plus de QR géant si la TV est rafraîchie en pleine partie).
- [x] **P0-5 HAUTE — pas de vrai fallback si le wifi bloque les WebSockets.**
  `socket-config.ts` : le commentaire « polling en fallback » est faux sans
  `tryAllTransports: true` (dispo dans socket.io-client 4.8.3 installé).
  **Fixé** : `tryAllTransports: true` + pingTimeout 5s→10s (faux « déconnecté »
  en rafale sur wifi saturé).
- [x] **P0-6 MOYENNE — payloads non bornés côté serveur.**
  Pseudo/réponse jusqu'à ~1 Mo, Levenshtein plein-matrice dessus (blocage event
  loop possible). **Fixé** : pseudo trim + 20 chars max (INVALID_PSEUDO si vide),
  réponse ≤ 200 chars / ordering ≤ 30 items / nombres finis (`invalid_answer`),
  garde de longueur avant Levenshtein (|Δlen| > tolérance → skip, zéro changement
  de comportement). NB : Socket.io coupe déjà la connexion > 1 Mo (maxHttpBufferSize).
- [x] **P0-7 MOYENNE — fin anticipée faussée + retardataires.**
  Un déconnecté sort du dénominateur « tous ont répondu » (question fermée pendant
  qu'il recharge) ; un retardataire ne reçoit pas la question en cours
  (`session_joined` sans question) et voit « Raté ❌ » sur une question jamais vue.
  **Fixé** : fenêtre de grâce 30 s dans le dénominateur (le timer serveur borne
  l'attente) ; la question en cours est rejouée au retardataire. Bonus :
  `timeElapsed` sur `question_started` → le chrono ne repart plus à fond après
  un refresh host/TV ni pour un retardataire.

## P1 — filet soirée

- [ ] **P1-1 — snapshot périodique des sessions + restore au boot.**
  Aujourd'hui : restart = partie totalement perdue (état 100 % mémoire).
  Cible : sauvegarde SQLite (Volume Railway), restauration au démarrage, question
  ouverte rejouée ; les tokens des téléphones redeviennent valides.
- [ ] **P1-2 — clé host secrète + join TV dédié.**
  `host_join` ne demande que le PIN affiché sur la TV (n'importe qui peut prendre
  la main) ; un PIN inconnu CRÉE une session silencieuse avec le plus vieux quiz.
- [ ] **P1-3 — filets host : kick + rejouer la dernière question.**
- [ ] **P1-4 — barème `closest` robuste aux réponses aberrantes + tolérance
  Levenshtein proportionnelle à la longueur** (« 1998 » ≠ « 1898 »).
- [ ] **P1-5 — divers** : préchargement image suivante (host/TV), titre du quiz
  visible sur host control, `railway.json` maxRetries 5→10, bouton « nouvelle
  session » après un quiz terminé, transaction sur createQuiz/replaceQuiz
  (questions insérées HORS transaction aujourd'hui — un crash peut vider un quiz).

## Constats acceptés en l'état (contexte soirée interne)

- PIN 4 chiffres brute-forçable, pas de rate-limit join — interne, risque faible.
- `/api/sessions/:id/report` non authentifié (id = uuid non devinable).
- Comparaison ADMIN_PASSWORD non timing-safe — accessoire en interne.
- Doublon de pseudo possible avec un joueur déconnecté (rare, bénin).
- `cleanupDisconnectedParticipants` jamais appelée = **voulu** : la fenêtre de
  reconnexion est illimitée (le TTL 30 s de `socket-config.ts` est du code mort).

## Checklist opérationnelle du soir J (hors code)

- [ ] Déploiement FIGÉ 24 h avant (aucun redeploy pendant la soirée — perte de partie).
- [ ] `ADMIN_PASSWORD` défini sur Railway ; quiz de la soirée sélectionné explicitement.
- [ ] Répétition générale avec 5-10 vrais téléphones **sur le wifi du lieu**.
- [ ] Hotspot 4G de secours prêt pour le Mac host.
- [ ] Onglet `/health` ouvert pendant la soirée (nb sessions + sha build).
- [ ] Test de charge simulant 80 joueurs avant le jour J.
